def parse(raw):
    # Placeholder — adapt to ARMAC format
    candles = []
    return candles
def parse_armac(raw):
    """
    Convert ARMAC timelapse raw text into candle objects.
    Placeholder until ARMAC format is confirmed.
    """
    candles = []

    # TODO: adapt to your ARMAC format
    # Example placeholder:
    # for line in raw.splitlines():
    #     parts = line.split(',')
    #     candles.append({
    #         "time": parts[0],
    #         "open": float(parts[1]),
    #         "high": float(parts[2]),
    #         "low": float(parts[3]),
    #         "close": float(parts[4]),
    #         "volume": float(parts[5])
    #     })

    return candles
